This source skill directory does not need to track platform binaries.

During installation, `bridgectl -install-skill` materializes the current platform executables into this folder:

- `bridgectl(.exe)`
- `jupyterbridge-mcp(.exe)`

Release bundles may also ship prebuilt binaries under `bin/<os-arch>/` before installation.
